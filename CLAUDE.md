# Dotfiles

Git repo at `~/.config/` tracking tmux, nvim, and ghostty configs.

## Remotes

Two remotes — push to both when committing changes:

```bash
git push origin main && git push personal main
```

- `origin` — `mgiampaolo_meli/dotfiles` (work GitHub, Enterprise Managed User)
- `personal` — `Jantalus/dotfiles` (personal GitHub, SSH alias `github.com-facu`)
